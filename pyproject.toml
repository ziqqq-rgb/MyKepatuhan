[project]
name = "mykepatuhan"
version = "0.1.0"
description = "RAG-based compliance assistant for Malaysian entrepreneurs"
readme = "README.md"
requires-python = ">=3.13"

[tool.uv.sources]
# dependencies managed via requirements.txt